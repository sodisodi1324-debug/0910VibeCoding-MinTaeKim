# DESIGN

## 이게 뭔가요
김민태의 개인 소개를 위한 한 페이지 웹사이트다. 이름과 소개 글, 관심사, 연락/외부 링크를 한 화면에서 보여준다. 방문자는 스크롤만으로 소개 → 관심사 → 링크 순서로 훑어볼 수 있다.

## 화면 구성
가운데 정렬된 좁은 한 단(column)에 위에서 아래로 세 블록이 얇은 구분선으로 나뉘어 쌓인다.

1. **소개 블록** — 이름(아주 큰 제목), 한 줄 소개, 자기소개 본문 몇 문장
2. **관심사 블록** — "관심사" 라벨 아래, 관심사들이 둥근 알약(pill) 형태로 나열
3. **링크 블록** — "링크" 라벨 아래, GitHub·Email 등 외부 링크가 한 줄씩 나열 (좌측 이름 / 우측 상세 정보), 각 줄은 얇은 구분선으로 분리, 클릭 시 새 탭으로 이동

## 지금 쓰는 색
- **배경**: 거의 흰색에 가까운 밝은 회백색(#fbfbfd)
- **본문 글자**: 짙은 회색에 가까운 검정(#1d1d1f)
- **보조 글자**(태그라인, 라벨, 상세 정보): 중간 회색(#6e6e73)
- **링크**: 파랑(#0071e3), 호버 시 진한 파랑(#0058b0)
- **알약 배경**: 옅은 회색(#f0f0f2)
- **구분선**: 아주 옅은 검은색 반투명선 (rgba(0,0,0,.08))

여백과 얇은 선 위주로 절제된 톤을 유지하고, 색은 텍스트와 링크에만 최소한으로 사용한다.

## 지금 쓰는 글꼴
- 시스템 폰트 우선: `-apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo"` — macOS/iOS에서는 San Francisco가 그대로 쓰인다
- 시스템 폰트가 없는 환경(Windows 등)을 위한 대체 웹폰트: **Noto Sans KR** (Google Fonts)

## 전체적인 느낌
애플 공식 사이트류의 절제된 타이포그래피 중심 디자인. 장식 요소 없이 큰 여백, 얇은 구분선, 넉넉한 자간/행간만으로 위계를 만든다.
